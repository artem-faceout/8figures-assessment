[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]

[tool.coverage.run]
source = ["routers", "services", "models", "data"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
fail_under = 80
